rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Daily price CSV snapshots — public read, no public write
    match /prices/{date} {
      allow read: if true;
      allow write: if false;
    }
  }
}
